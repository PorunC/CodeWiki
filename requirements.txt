# Python utility-script dependencies.
#
# The backend is the TypeScript/npm package in backend. Python is only used
# for local helper scripts and their pytest coverage.
pytest>=8.3.0
ruff>=0.8.0
tqdm>=4.67.0
